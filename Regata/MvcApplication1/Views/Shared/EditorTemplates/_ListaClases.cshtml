@model List<MvcApplication1.Models.ClaseViewModel>

<div>
   @for (int i = 0; i < Model.Count; i++)
      {
          @Html.HiddenFor(model=>model[i].Nombre)
          @Html.CheckBoxFor(m => m[i].Seleccionado, new { id = i })
          @Html.DisplayFor(m => m[i].Nombre)
      }
    
</div>

